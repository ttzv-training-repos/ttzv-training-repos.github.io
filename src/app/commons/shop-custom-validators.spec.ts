import { ShopCustomValidators } from './shop-custom-validators';

describe('ShopCustomValidators', () => {
  it('should create an instance', () => {
    expect(new ShopCustomValidators()).toBeTruthy();
  });
});
